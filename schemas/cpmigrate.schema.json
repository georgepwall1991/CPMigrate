{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/georgepwall1991/CPMigrate/main/schemas/cpmigrate.schema.json",
  "title": "CPMigrate Configuration",
  "description": "Configuration file for CPMigrate - Central Package Management migration tool",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON schema reference for IDE autocomplete"
    },
    "conflictStrategy": {
      "type": "string",
      "enum": ["Highest", "Lowest", "Fail"],
      "default": "Highest",
      "description": "How to handle version conflicts: Highest (use latest), Lowest (use earliest), or Fail (stop migration)"
    },
    "backup": {
      "type": "boolean",
      "default": true,
      "description": "Whether to create backups before modifying files"
    },
    "backupDir": {
      "type": "string",
      "default": ".cpmigrate_backup",
      "description": "Directory for backup files"
    },
    "addGitignore": {
      "type": "boolean",
      "default": false,
      "description": "Whether to add backup directory to .gitignore"
    },
    "keepVersionAttributes": {
      "type": "boolean",
      "default": false,
      "description": "Whether to keep Version attributes in project files after migration"
    },
    "mergeExisting": {
      "type": "boolean",
      "default": false,
      "description": "Whether to merge into an existing Directory.Packages.props file"
    },
    "outputFormat": {
      "type": "string",
      "enum": ["Terminal", "Json", "Sarif", "Markdown", "Csv"],
      "default": "Terminal",
      "description": "Output format: Terminal (rich UI), Json (for CI/CD), Sarif (GitHub code scanning), Markdown (CI job summary or PR comment), or Csv (spreadsheet analysis). Sarif, Markdown, and Csv require --analyze."
    },
    "baseline": {
      "type": "string",
      "default": ".cpmigrate-baseline.json",
      "description": "Path to the accepted-findings baseline. Findings it records are reported but do not fail the build, so CI gates on new problems only. Generate with: cpmigrate --analyze --write-baseline"
    },
    "failOn": {
      "type": "string",
      "enum": ["Info", "Low", "Moderate", "High", "Critical", "Never"],
      "default": "Info",
      "description": "Lowest finding severity that fails the build during --analyze. Info fails on any finding; Never reports without gating. Does not suppress exit 8 for an incomplete scan."
    },
    "verify": {
      "type": "boolean",
      "default": false,
      "description": "Whether every migration must prove it did not move the resolved dependency graph. Restores before and after, diffs the result, and exits 9 on drift nothing accounts for — rolling the migration back. Costs two full solution restores. Team-wide for the same reason failOn is: a policy that has to be remembered as a flag is one that gets forgotten on the run that mattered."
    },
    "verifyStrict": {
      "type": "boolean",
      "default": false,
      "description": "Whether a verified migration must be a literal no-op, failing even on drift the receipt can explain. Requires verify."
    },
    "rules": {
      "type": "object",
      "description": "Per-rule policy applied before --fail-on. Each key is a rule ID as published in JSON issueCode and SARIF ruleId (run 'cpmigrate --explain all' to list them); each value is either a severity or 'none' to switch the rule off. A disabled rule reports nothing at all, unlike a baseline, which keeps accepted findings visible. Rule IDs and severities are matched case-insensitively; the spellings listed here are the canonical ones. Names are listed as properties rather than constrained with propertyNames so an editor offers them without a differently-cased but perfectly valid policy failing schema validation — an unknown rule ID is rejected by the tool itself, with a message naming it.",
      "properties": {
        "Unknown": { "$ref": "#/definitions/rulePolicyValue" },
        "VersionInconsistency": { "$ref": "#/definitions/rulePolicyValue" },
        "DuplicatePackageCasing": { "$ref": "#/definitions/rulePolicyValue" },
        "RedundantReference": { "$ref": "#/definitions/rulePolicyValue" },
        "TransitiveConflict": { "$ref": "#/definitions/rulePolicyValue" },
        "SecurityVulnerability": { "$ref": "#/definitions/rulePolicyValue" },
        "RedundantDirectReference": { "$ref": "#/definitions/rulePolicyValue" },
        "FrameworkAlignment": { "$ref": "#/definitions/rulePolicyValue" },
        "OutdatedPackage": { "$ref": "#/definitions/rulePolicyValue" },
        "DeprecatedPackage": { "$ref": "#/definitions/rulePolicyValue" },
        "CpmNotEnabled": { "$ref": "#/definitions/rulePolicyValue" },
        "InlineVersionUnderCpm": { "$ref": "#/definitions/rulePolicyValue" },
        "MissingPackageVersion": { "$ref": "#/definitions/rulePolicyValue" },
        "OrphanedPackageVersion": { "$ref": "#/definitions/rulePolicyValue" },
        "LicenseRisk": { "$ref": "#/definitions/rulePolicyValue" },
        "FloatingVersion": { "$ref": "#/definitions/rulePolicyValue" },
        "EolTargetFramework": { "$ref": "#/definitions/rulePolicyValue" }
      },
      "additionalProperties": { "$ref": "#/definitions/rulePolicyValue" }
    },
    "retention": {
      "type": "object",
      "description": "Backup retention settings",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Whether automatic backup retention is enabled"
        },
        "maxBackups": {
          "type": "integer",
          "minimum": 1,
          "default": 5,
          "description": "Maximum number of backups to keep"
        }
      },
      "additionalProperties": false
    },
    "excludeDirectories": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": ["node_modules", "bin", "obj", ".git", "packages"],
      "description": "Directories to exclude when scanning for solutions in batch mode"
    },
    "analyze": {
      "type": "boolean",
      "default": false,
      "description": "Whether every run analyzes instead of migrating. Team-wide for the same reason failOn is: 'this repo is under CPM already; check it, don't convert it' is a policy, and a policy that has to be remembered as a flag is one that gets forgotten."
    },
    "transitive": {
      "type": "boolean",
      "default": false,
      "description": "Whether analysis includes transitive dependencies. Costs a restore per project, so it is a team decision about how deep the default scan goes."
    },
    "audit": {
      "type": "boolean",
      "default": false,
      "description": "Whether analysis includes the security audit. Costs network lookups, so it is a team decision about what the default scan covers."
    },
    "outdated": {
      "type": "boolean",
      "default": false,
      "description": "Whether analysis includes outdated-package checks."
    },
    "deprecated": {
      "type": "boolean",
      "default": false,
      "description": "Whether analysis includes deprecated-package checks."
    },
    "licenses": {
      "type": "boolean",
      "default": false,
      "description": "Whether analysis includes license checks."
    },
    "maxParallelism": {
      "type": "integer",
      "minimum": 1,
      "description": "Maximum projects scanned at once. A team decision about how hard the scan pushes the machine — CI runners and developer laptops want different answers."
    }
  },
  "definitions": {
    "rulePolicyValue": {
      "type": "string",
      "description": "A severity to re-grade the rule to, or 'none' to switch the rule off. The tool trims surrounding whitespace and matches case-insensitively, so the pattern below accepts any casing while the enum branch is what an editor offers for completion. Listing only canonical spellings would reject a config the tool runs perfectly well.",
      "anyOf": [
        {
          "enum": ["Info", "Low", "Moderate", "High", "Critical", "none"]
        },
        {
          "pattern": "^\\s*([Ii][Nn][Ff][Oo]|[Ll][Oo][Ww]|[Mm][Oo][Dd][Ee][Rr][Aa][Tt][Ee]|[Hh][Ii][Gg][Hh]|[Cc][Rr][Ii][Tt][Ii][Cc][Aa][Ll]|[Nn][Oo][Nn][Ee])\\s*$"
        }
      ]
    }
  },
  "additionalProperties": false
}
