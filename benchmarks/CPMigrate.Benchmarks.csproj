<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <RollForward>LatestMajor</RollForward>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <!-- Deliberately not referenced by CPMigrate.sln: this is a manual measurement tool, not CI
         surface. Run it with `dotnet run -p benchmarks` from the repository root. -->
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\CPMigrate\CPMigrate.csproj" />
  </ItemGroup>
</Project>
